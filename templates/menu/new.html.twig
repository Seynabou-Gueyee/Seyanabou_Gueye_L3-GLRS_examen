{% extends 'base.html.twig' %}

{% block title %}Ajouter un Menu{% endblock %}

{% block body %}
<div class="page-header">
    <h1><i class="fas fa-utensils"></i> Ajouter un Menu</h1>
    <a href="{{ path('app_menu_index') }}" class="btn btn-secondary">
        <i class="fas fa-arrow-left"></i> Retour
    </a>
</div>

<div class="card" style="max-width: 800px;">
    <div class="card-body">
        {{ form_start(form) }}
            <div class="form-group">
                {{ form_label(form.nom) }}
                {{ form_widget(form.nom) }}
                {{ form_errors(form.nom) }}
            </div>
            <div class="form-group">
                {{ form_label(form.burgers) }}
                {{ form_widget(form.burgers) }}
                {{ form_errors(form.burgers) }}
            </div>
            <div class="form-group">
                {{ form_label(form.complements) }}
                {{ form_widget(form.complements) }}
                {{ form_errors(form.complements) }}
            </div>
            <div class="form-group">
                {{ form_label(form.image) }}
                {{ form_widget(form.image) }}
                {{ form_errors(form.image) }}
            </div>
            <div style="margin-top: 30px; display: flex; gap: 10px;">
                <button type="submit" class="btn btn-primary">
                    <i class="fas fa-save"></i> Enregistrer
                </button>
                <a href="{{ path('app_menu_index') }}" class="btn btn-secondary">
                    <i class="fas fa-times"></i> Annuler
                </a>
            </div>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
