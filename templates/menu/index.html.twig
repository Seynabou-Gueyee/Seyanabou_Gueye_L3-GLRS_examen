{% extends 'base.html.twig' %}

{% block title %}Gestion des Menus{% endblock %}

{% block body %}
<div class="page-header">
    <h1><i class="fas fa-utensils"></i> Gestion des Menus</h1>
    <a href="{{ path('app_menu_new') }}" class="btn btn-primary">
        <i class="fas fa-plus"></i> Ajouter un Menu
    </a>
</div>

<div class="card">
    <div class="table-responsive">
        <table>
            <thead>
                <tr>
                    <th>Image</th>
                    <th>Nom</th>
                    <th>Prix</th>
                    <th>État</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% if menus is empty %}
                <tr>
                    <td colspan="5" style="text-align: center; padding: 40px; color: #666;">
                        <i class="fas fa-inbox" style="font-size: 48px; margin-bottom: 10px; display: block;"></i>
                        Aucun menu disponible
                    </td>
                </tr>
                {% else %}
                    {% for menu in menus %}
                    <tr>
                        <td>
                            {% if menu.image %}
                                <img src="{{ asset('uploads/' ~ menu.image) }}" width="60" height="60" style="border-radius: 8px; object-fit: cover;">
                            {% else %}
                                <div style="width: 60px; height: 60px; background: #2a2a2a; border-radius: 8px; display: flex; align-items: center; justify-content: center;">
                                    <i class="fas fa-utensils" style="color: #666;"></i>
                                </div>
                            {% endif %}
                        </td>
                        <td><strong>{{ menu.nom }}</strong></td>
                        <td><span style="color: #ff9500; font-weight: 600;">{{ menu.prix }} FCFA</span></td>
                        <td>
                            {% if menu.archive %}
                                <span class="badge badge-danger">Archivé</span>
                            {% else %}
                                <span class="badge badge-success">Actif</span>
                            {% endif %}
                        </td>
                        <td>
                            <a href="{{ path('app_menu_edit', {id: menu.id}) }}" class="btn btn-sm btn-warning" title="Modifier">
                                <i class="fas fa-edit"></i>
                            </a>
                            <a href="{{ path('app_menu_archive', {id: menu.id}) }}" class="btn btn-sm btn-secondary" title="{{ menu.archive ? 'Désarchiver' : 'Archiver' }}">
                                <i class="fas fa-{{ menu.archive ? 'undo' : 'archive' }}"></i>
                            </a>
                        </td>
                    </tr>
                    {% endfor %}
                {% endif %}
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
